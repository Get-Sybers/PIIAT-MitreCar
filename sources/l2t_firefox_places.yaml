# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: l2t_firefox_places
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (sqlite/firefox_history (places.sqlite)).
  Coverage is generated by introspecting this repo's map `l2t_firefox_places` (piiat_mitrecar.mappings);
  regenerate after any map change.
derived_from: disk image (Firefox places.sqlite)
extractor:
  tool: log2timeline / Plaso
  parser: sqlite/firefox_history (places.sqlite)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tSqlite
identity:
  registry: piiat_mitrecar/spindle.yml
  version: 1
  entries:
  - name: l2t_firefox_places
    kind: entity
    scope: intrinsic
    version: 1
data_model_coverage: http
mappings:
- object: http
  action: get
  notes: Derived by log2timeline / Plaso — sqlite/firefox_history (places.sqlite).
  fields:
  - hostname
  - request_referrer
  - url_domain
  - url_full
  - url_remainder
  - url_scheme
  field_provenance:
    hostname: image_hostname [direct]
    request_referrer: from_visit | from_visit [coalesced]
    url_domain: url [inferred]
    url_full: url [direct]
    url_remainder: url [inferred]
    url_scheme: url [inferred]
other_coverage:
- 'native/join keys (http/get): artefact_file, data_type, title, typed, visit_count, visit_type'
