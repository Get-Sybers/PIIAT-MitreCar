# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: l2t_mft
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (mft (NTFS $MFT)). Coverage is generated
  by introspecting this repo's map `l2t_mft` (piiat_mitrecar.mappings); regenerate after any map change.
derived_from: disk image (NTFS $MFT)
extractor:
  tool: log2timeline / Plaso
  parser: mft (NTFS $MFT)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tMft
data_model_coverage: file
mappings:
- object: file
  action: create
  notes: Derived by log2timeline / Plaso — mft (NTFS $MFT).
  fields:
  - creation_time
  - extension
  - file_name
  - file_path
  - hostname
  - user
- object: file
  action: delete
  notes: Derived by log2timeline / Plaso — mft (NTFS $MFT).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  - user
- object: file
  action: modify
  notes: Derived by log2timeline / Plaso — mft (NTFS $MFT).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  - user
- object: file
  action: read
  notes: Derived by log2timeline / Plaso — mft (NTFS $MFT).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  - user
other_coverage:
- 'native/join keys (file/create): data_type, disk_id, display_name, file_attribute_flags, file_reference,
  is_allocated, parent_file_reference, path_hints, timestamp_desc, volume_id, volume_offset'
- 'native/join keys (file/delete): data_type, disk_id, display_name, file_attribute_flags, file_reference,
  is_allocated, parent_file_reference, path_hints, timestamp_desc, volume_id, volume_offset'
- 'native/join keys (file/modify): data_type, disk_id, display_name, file_attribute_flags, file_reference,
  is_allocated, parent_file_reference, path_hints, timestamp_desc, volume_id, volume_offset'
- 'native/join keys (file/read): data_type, disk_id, display_name, file_attribute_flags, file_reference,
  is_allocated, parent_file_reference, path_hints, timestamp_desc, volume_id, volume_offset'
