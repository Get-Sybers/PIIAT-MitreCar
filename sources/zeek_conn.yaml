# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: zeek_conn
sensor_version: 0.1.0
sensor_developer: The Zeek Project
sensor_url: https://zeek.org
sensor_description: CAR events derived by Zeek (conn.log). Coverage is generated by introspecting this
  repo's map `zeek_conn` (piiat_mitrecar.mappings); regenerate after any map change.
derived_from: network capture (pcap / live traffic)
extractor:
  tool: Zeek
  parser: conn.log
  url: https://zeek.org
input_pattern:
- conn.json
data_model_coverage: flow
mappings:
- object: flow
  action: end
  notes: Derived by Zeek — conn.log.
  fields:
  - application_protocol
  - dest_ip
  - dest_port
  - end_time
  - in_bytes
  - out_bytes
  - packet_count
  - src_ip
  - src_port
  - start_time
  - tcp_flags
  - transport_protocol
  field_provenance:
    application_protocol: service [direct]
    dest_ip: id.resp_h [direct]
    dest_port: id.resp_p [direct]
    end_time: _zc_end_time [direct]
    in_bytes: resp_bytes [direct]
    out_bytes: orig_bytes [direct]
    packet_count: _zc_packet_count [direct]
    src_ip: id.orig_h [direct]
    src_port: id.orig_p [direct]
    start_time: ts [derived]
    tcp_flags: history [direct]
    transport_protocol: proto [direct]
- object: flow
  action: message
  notes: Derived by Zeek — conn.log.
  fields:
  - application_protocol
  - dest_ip
  - dest_port
  - end_time
  - in_bytes
  - out_bytes
  - packet_count
  - src_ip
  - src_port
  - start_time
  - tcp_flags
  - transport_protocol
  field_provenance:
    application_protocol: service [direct]
    dest_ip: id.resp_h [direct]
    dest_port: id.resp_p [direct]
    end_time: _zc_end_time [direct]
    in_bytes: resp_bytes [direct]
    out_bytes: orig_bytes [direct]
    packet_count: _zc_packet_count [direct]
    src_ip: id.orig_h [direct]
    src_port: id.orig_p [direct]
    start_time: ts [derived]
    tcp_flags: history [direct]
    transport_protocol: proto [direct]
- object: flow
  action: start
  notes: Derived by Zeek — conn.log.
  fields:
  - application_protocol
  - dest_ip
  - dest_port
  - end_time
  - in_bytes
  - out_bytes
  - packet_count
  - src_ip
  - src_port
  - start_time
  - tcp_flags
  - transport_protocol
  field_provenance:
    application_protocol: service [direct]
    dest_ip: id.resp_h [direct]
    dest_port: id.resp_p [direct]
    end_time: _zc_end_time [direct]
    in_bytes: resp_bytes [direct]
    out_bytes: orig_bytes [direct]
    packet_count: _zc_packet_count [direct]
    src_ip: id.orig_h [direct]
    src_port: id.orig_p [direct]
    start_time: ts [derived]
    tcp_flags: history [direct]
    transport_protocol: proto [direct]
other_coverage: []
