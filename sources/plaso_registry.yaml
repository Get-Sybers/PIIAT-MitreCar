# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: plaso_registry
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (winreg (registry key/value plugins)).
  Coverage is generated by introspecting this repo's map `plaso_registry` (piiat_mitrecar.mappings); regenerate
  after any map change.
derived_from: disk image (Windows registry hives)
extractor:
  tool: log2timeline / Plaso
  parser: winreg (registry key/value plugins)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tWinreg
data_model_coverage: registry
mappings:
- object: registry
  action: key_edit
  notes: Derived by log2timeline / Plaso — winreg (registry key/value plugins).
  fields:
  - hive
  - hostname
  - image_path
  - key
  field_provenance:
    hive: display_name [direct]
    hostname: image_hostname [direct]
    image_path: image_path [direct]
    key: key_path [direct]
other_coverage:
- 'native/join keys (registry/key_edit): account_rid, application, build_number, command, comments, configuration,
  data_type, device_display_name, device_type, drive_letter, entries, error_control, fullname, handler,
  hive_user_sid, login_count, name, object_name, owner, product, product_name, revision, serial, server_name,
  service_dll, service_pack, service_type, settings, share_name, source_type, start_type, subkey_name,
  trigger, username, values, vendor, version'
