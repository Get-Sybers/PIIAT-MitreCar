# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: plaso_shellitem
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (shell items (embedded, e.g. in lnk/registry)).
  Coverage is generated by introspecting this repo's map `plaso_shellitem` (piiat_mitrecar.mappings);
  regenerate after any map change.
derived_from: disk image (embedded shell items)
extractor:
  tool: log2timeline / Plaso
  parser: shell items (embedded, e.g. in lnk/registry)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tLnk
- .L2tWinreg
data_model_coverage: file
mappings:
- object: file
  action: create
  notes: Derived by log2timeline / Plaso — shell items (embedded, e.g. in lnk/registry).
  fields:
  - creation_time
  - extension
  - file_name
  - file_path
  - hostname
- object: file
  action: modify
  notes: Derived by log2timeline / Plaso — shell items (embedded, e.g. in lnk/registry).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
- object: file
  action: read
  notes: Derived by log2timeline / Plaso — shell items (embedded, e.g. in lnk/registry).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
other_coverage:
- 'native/join keys (file/create): artefact_path, artefact_sha256, data_type, disk_id, localized_name,
  long_name, name, origin, shell_item_path, volume_id, volume_offset'
- 'native/join keys (file/modify): artefact_path, artefact_sha256, data_type, disk_id, localized_name,
  long_name, name, origin, shell_item_path, volume_id, volume_offset'
- 'native/join keys (file/read): artefact_path, artefact_sha256, data_type, disk_id, localized_name, long_name,
  name, origin, shell_item_path, volume_id, volume_offset'
