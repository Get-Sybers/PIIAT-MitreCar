# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: zeek_smtp
sensor_version: 0.1.0
sensor_developer: The Zeek Project
sensor_url: https://zeek.org
sensor_description: CAR events derived by Zeek (smtp.log). Coverage is generated by introspecting this
  repo's map `zeek_smtp` (piiat_mitrecar.mappings); regenerate after any map change.
derived_from: network capture (pcap / live traffic)
extractor:
  tool: Zeek
  parser: smtp.log
  url: https://zeek.org
input_pattern:
- smtp.json
data_model_coverage: email
mappings:
- object: email
  action: deliver
  notes: Derived by Zeek — smtp.log.
  fields:
  - date
  - dest_address
  - dest_ip
  - dest_port
  - from
  - src_address
  - src_domain
  - src_ip
  - src_port
  - subject
  - to
  field_provenance:
    date: date [direct]
    dest_address: rcptto | to [coalesced]
    dest_ip: id.resp_h [direct]
    dest_port: id.resp_p [direct]
    from: from [direct]
    src_address: mailfrom | from [coalesced]
    src_domain: mailfrom | from [derived]
    src_ip: id.orig_h [direct]
    src_port: id.orig_p [direct]
    subject: subject [direct]
    to: to [direct]
other_coverage: []
