# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: memory
sensor_version: 0.1.0
sensor_developer: Get-Sybers / Volatility Foundation
sensor_url: https://github.com/Get-Sybers/PIIAT-Mem
sensor_description: PIIAT-Mem normalises a memory image directly into finished MITRE CAR (its own car.db);
  the pipeline passes those events through 1:1 (no re-mapping here). Object/action/field coverage is defined
  by PIIAT-Mem, not by this repo's maps, so it is not enumerated here.
derived_from: memory image
extractor:
  tool: PIIAT-Mem (Volatility 3)
  parser: memory image (car.db)
  url: https://github.com/Get-Sybers/PIIAT-Mem
input_pattern:
- car.db
identity:
  external:
  - memory_proc_offset
data_model_coverage: (defined by the producing tool; passthrough)
mappings: []
other_coverage:
- Derived by PIIAT-Mem (Volatility 3) — memory image (car.db) (finished-CAR passthrough).
