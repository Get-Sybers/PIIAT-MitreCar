# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: plaso_fseventsd
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (fseventsd (macOS FSEvents)). Coverage
  is generated by introspecting this repo's map `plaso_fseventsd` (piiat_mitrecar.mappings); regenerate
  after any map change.
derived_from: disk image (macOS fseventsd)
extractor:
  tool: log2timeline / Plaso
  parser: fseventsd (macOS FSEvents)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tFseventsd
data_model_coverage: file
mappings:
- object: file
  action: modify
  notes: Derived by log2timeline / Plaso — fseventsd (macOS FSEvents).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  field_provenance:
    extension: path [derived]
    file_name: path [derived]
    file_path: path [direct]
    hostname: image_hostname [direct]
other_coverage:
- 'native/join keys (file/modify): artefact_sha256, data_type, disk_id, event_identifier, flags, node_identifier,
  timestamp_desc, volume_id'
