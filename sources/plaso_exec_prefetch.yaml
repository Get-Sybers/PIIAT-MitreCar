# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: plaso_exec_prefetch
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (prefetch (Windows Prefetch)). Coverage
  is generated by introspecting this repo's map `plaso_exec_prefetch` (piiat_mitrecar.mappings); regenerate
  after any map change.
derived_from: disk image (Windows Prefetch)
extractor:
  tool: log2timeline / Plaso
  parser: prefetch (Windows Prefetch)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tPrefetch
identity:
  registry: piiat_mitrecar/spindle.yml
  version: 1
  entries:
  - name: plaso_exec_prefetch
    kind: entity
    scope: intrinsic
    version: 1
data_model_coverage: process
mappings:
- object: process
  action: create
  notes: Derived by log2timeline / Plaso — prefetch (Windows Prefetch).
  fields:
  - exe
  - hostname
  - user
  field_provenance:
    exe: executable [direct]
    hostname: image_hostname [direct]
    user: username [direct]
other_coverage:
- 'native/join keys (process/create): artefact_file, artefact_sha256, data_type, mapped_files, number_of_volumes,
  path_hints, prefetch_hash, run_count, timestamp_desc, version, volume_device_paths, volume_serial_numbers'
