# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: l2t_lnk
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (lnk (Windows Shortcut)). Coverage is generated
  by introspecting this repo's map `l2t_lnk` (piiat_mitrecar.mappings); regenerate after any map change.
derived_from: disk image (LNK shortcut files)
extractor:
  tool: log2timeline / Plaso
  parser: lnk (Windows Shortcut)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tLnk
identity:
  registry: piiat_mitrecar/spindle.yml
  version: 1
  entries:
  - name: l2t_lnk
    kind: entity
    scope: intrinsic
    version: 1
data_model_coverage: file
mappings:
- object: file
  action: create
  notes: Derived by log2timeline / Plaso — lnk (Windows Shortcut).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  - link_target
  - user
  field_provenance:
    extension: local_path | network_path | link_target [derived]
    file_name: local_path | network_path | link_target [derived]
    file_path: local_path | network_path | link_target [derived]
    hostname: image_hostname [direct]
    link_target: link_target [direct]
    user: username [direct]
- object: file
  action: modify
  notes: Derived by log2timeline / Plaso — lnk (Windows Shortcut).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  - link_target
  - user
  field_provenance:
    extension: local_path | network_path | link_target [derived]
    file_name: local_path | network_path | link_target [derived]
    file_path: local_path | network_path | link_target [derived]
    hostname: image_hostname [direct]
    link_target: link_target [direct]
    user: username [direct]
- object: file
  action: read
  notes: Derived by log2timeline / Plaso — lnk (Windows Shortcut).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  - link_target
  - user
  field_provenance:
    extension: local_path | network_path | link_target [derived]
    file_name: local_path | network_path | link_target [derived]
    file_path: local_path | network_path | link_target [derived]
    hostname: image_hostname [direct]
    link_target: link_target [direct]
    user: username [direct]
other_coverage:
- 'native/join keys (file/create): birth_droid_file_identifier, birth_droid_volume_identifier, command_line_arguments,
  data_type, description, drive_serial_number, drive_type, droid_file_identifier, droid_volume_identifier,
  env_var_location, file_attribute_flags, file_size, icon_location, link_target_raw, lnk_file, relative_path,
  volume_label, working_directory'
- 'native/join keys (file/modify): birth_droid_file_identifier, birth_droid_volume_identifier, command_line_arguments,
  data_type, description, drive_serial_number, drive_type, droid_file_identifier, droid_volume_identifier,
  env_var_location, file_attribute_flags, file_size, icon_location, link_target_raw, lnk_file, relative_path,
  volume_label, working_directory'
- 'native/join keys (file/read): birth_droid_file_identifier, birth_droid_volume_identifier, command_line_arguments,
  data_type, description, drive_serial_number, drive_type, droid_file_identifier, droid_volume_identifier,
  env_var_location, file_attribute_flags, file_size, icon_location, link_target_raw, lnk_file, relative_path,
  volume_label, working_directory'
