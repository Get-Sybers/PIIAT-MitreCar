# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: l2t_msiecf
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (msiecf (MS IE Cache File / index.dat)).
  Coverage is generated by introspecting this repo's map `l2t_msiecf` (piiat_mitrecar.mappings); regenerate
  after any map change.
derived_from: disk image (IE index.dat)
extractor:
  tool: log2timeline / Plaso
  parser: msiecf (MS IE Cache File / index.dat)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tMsiecf
data_model_coverage: http
mappings:
- object: http
  action: get
  notes: Derived by log2timeline / Plaso — msiecf (MS IE Cache File / index.dat).
  fields:
  - hostname
  - url_domain
  - url_full
  - url_remainder
  - url_scheme
other_coverage:
- 'native/join keys (http/get): artefact_file, data_type, number_of_hits, raw_url'
