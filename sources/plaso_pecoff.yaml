# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: plaso_pecoff
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (pe (PE/COFF metadata)). Coverage is generated
  by introspecting this repo's map `plaso_pecoff` (piiat_mitrecar.mappings); regenerate after any map
  change.
derived_from: disk image (PE/COFF executables)
extractor:
  tool: log2timeline / Plaso
  parser: pe (PE/COFF metadata)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tPe
data_model_coverage: file
mappings:
- object: file
  action: create
  notes: Derived by log2timeline / Plaso — pe (PE/COFF metadata).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  - sha256_hash
  field_provenance:
    extension: display_name | display_name [derived]
    file_name: display_name | display_name [derived]
    file_path: display_name | display_name [coalesced]
    hostname: image_hostname [direct]
    sha256_hash: sha256_hash [direct]
other_coverage:
- 'native/join keys (file/create): compile_time, data_type, disk_id, export_dll_name, imphash, pe_table_time,
  pe_type, section_names, timestamp_desc, volume_id, volume_offset'
