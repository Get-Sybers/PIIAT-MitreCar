# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: l2t_utmp
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (utmp (Linux login records)). Coverage
  is generated by introspecting this repo's map `l2t_utmp` (piiat_mitrecar.mappings); regenerate after
  any map change.
derived_from: disk image (utmp login database)
extractor:
  tool: log2timeline / Plaso
  parser: utmp (Linux login records)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tUtmp
data_model_coverage: user_session
mappings:
- object: user_session
  action: login
  notes: Derived by log2timeline / Plaso — utmp (Linux login records).
  fields:
  - hostname
  - src_ip
  - user
- object: user_session
  action: logout
  notes: Derived by log2timeline / Plaso — utmp (Linux login records).
  fields:
  - hostname
  - src_ip
  - user
other_coverage:
- 'native/join keys (user_session/login): data_type, exit_status, hostname, login_type, terminal, terminal_identifier'
- 'native/join keys (user_session/logout): data_type, exit_status, hostname, login_type, terminal, terminal_identifier'
