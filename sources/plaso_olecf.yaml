# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: plaso_olecf
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (olecf (OLE Compound File summary info)).
  Coverage is generated by introspecting this repo's map `plaso_olecf` (piiat_mitrecar.mappings); regenerate
  after any map change.
derived_from: disk image (OLE compound documents)
extractor:
  tool: log2timeline / Plaso
  parser: olecf (OLE Compound File summary info)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tOlecf
data_model_coverage: file
mappings:
- object: file
  action: create
  notes: Derived by log2timeline / Plaso — olecf (OLE Compound File summary info).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  - owner
  - sha256_hash
- object: file
  action: modify
  notes: Derived by log2timeline / Plaso — olecf (OLE Compound File summary info).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  - owner
  - sha256_hash
other_coverage:
- 'native/join keys (file/create): application, author, codepage, comments, data_type, disk_id, keywords,
  last_saved_by, number_of_characters, number_of_pages, number_of_words, revision_number, security_flags,
  subject, template, timestamp_desc, title, volume_id, volume_offset'
- 'native/join keys (file/modify): application, author, codepage, comments, data_type, disk_id, keywords,
  last_saved_by, number_of_characters, number_of_pages, number_of_words, revision_number, security_flags,
  subject, template, timestamp_desc, title, volume_id, volume_offset'
