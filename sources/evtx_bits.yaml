# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: evtx_bits
sensor_version: 0.1.0
sensor_developer: Eric Zimmerman (EZ Tools)
sensor_url: https://github.com/EricZimmerman/evtx
sensor_description: 'CAR events derived by EvtxECmd (Windows Event Log (.evtx) — Microsoft-Windows-Bits-Client/Operational
  (59/60)). Alternate derivation(s): log2timeline / Plaso winevt/winevtx parser (via the l2t_winevt adapter).
  Coverage is generated by introspecting this repo''s map `evtx_bits` (piiat_mitrecar.mappings); regenerate
  after any map change.'
derived_from: raw Windows Event Log (.evtx)
extractor:
  tool: EvtxECmd
  parser: Windows Event Log (.evtx) — Microsoft-Windows-Bits-Client/Operational (59/60)
  url: https://github.com/EricZimmerman/evtx
  alternates:
  - log2timeline / Plaso winevt/winevtx parser (via the l2t_winevt adapter)
input_pattern:
- .L2tWinevt
- .L2tWinevtx
- _EvtxECmd_Output
data_model_coverage: http
mappings:
- object: http
  action: get
  notes: Derived by EvtxECmd — Windows Event Log (.evtx) — Microsoft-Windows-Bits-Client/Operational (59/60).
  fields:
  - hostname
  - response_body_bytes
  - url_domain
  - url_full
  - url_remainder
  - url_scheme
  field_provenance:
    hostname: Computer [derived]
    response_body_bytes: bytesTransferred [direct]
    url_domain: url [inferred]
    url_full: url [direct]
    url_remainder: url [inferred]
    url_scheme: url [inferred]
other_coverage:
- 'native/join keys (http/get): bytesTotal, fileTime, name, peer, transferId'
