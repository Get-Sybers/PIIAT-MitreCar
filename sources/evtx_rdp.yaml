# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: evtx_rdp
sensor_version: 0.1.0
sensor_developer: Eric Zimmerman (EZ Tools)
sensor_url: https://github.com/EricZimmerman/evtx
sensor_description: 'CAR events derived by EvtxECmd (Windows Event Log (.evtx) — TerminalServices channels
  (21/24/25)). Alternate derivation(s): log2timeline / Plaso winevt/winevtx parser (via the l2t_winevt
  adapter). Coverage is generated by introspecting this repo''s map `evtx_rdp` (piiat_mitrecar.mappings);
  regenerate after any map change.'
derived_from: raw Windows Event Log (.evtx)
extractor:
  tool: EvtxECmd
  parser: Windows Event Log (.evtx) — TerminalServices channels (21/24/25)
  url: https://github.com/EricZimmerman/evtx
  alternates:
  - log2timeline / Plaso winevt/winevtx parser (via the l2t_winevt adapter)
input_pattern:
- .L2tWinevt
- .L2tWinevtx
- _EvtxECmd_Output
data_model_coverage: user_session
mappings:
- object: user_session
  action: login
  notes: Derived by EvtxECmd — Windows Event Log (.evtx) — TerminalServices channels (21/24/25).
  fields:
  - hostname
  - src_ip
  - user
  field_provenance:
    hostname: Computer [derived]
    src_ip: Address [inferred]
    user: User [direct]
- object: user_session
  action: logout
  notes: Derived by EvtxECmd — Windows Event Log (.evtx) — TerminalServices channels (21/24/25).
  fields:
  - hostname
  - src_ip
  - user
  field_provenance:
    hostname: Computer [derived]
    src_ip: Address [inferred]
    user: User [direct]
- object: user_session
  action: reconnect
  notes: Derived by EvtxECmd — Windows Event Log (.evtx) — TerminalServices channels (21/24/25).
  fields:
  - hostname
  - src_ip
  - user
  field_provenance:
    hostname: Computer [derived]
    src_ip: Address [inferred]
    user: User [direct]
other_coverage:
- 'native/join keys (user_session/login): Address, SessionID'
- 'native/join keys (user_session/logout): Address, SessionID'
- 'native/join keys (user_session/reconnect): Address, SessionID'
