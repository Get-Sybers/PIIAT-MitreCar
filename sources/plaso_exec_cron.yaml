# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: plaso_exec_cron
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (syslog/cron (task-run lines)). Coverage
  is generated by introspecting this repo's map `plaso_exec_cron` (piiat_mitrecar.mappings); regenerate
  after any map change.
derived_from: disk image (cron/syslog text logs)
extractor:
  tool: log2timeline / Plaso
  parser: syslog/cron (task-run lines)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tCron
- .L2tSyslog
data_model_coverage: process
mappings:
- object: process
  action: create
  notes: Derived by log2timeline / Plaso — syslog/cron (task-run lines).
  fields:
  - command_line
  - exe
  - hostname
  - image_path
  - pid
  - user
other_coverage:
- 'native/join keys (process/create): artefact_file, artefact_sha256, data_type, message_body, reporter,
  syslog_hostname, timestamp_desc'
