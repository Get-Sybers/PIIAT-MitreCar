# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: l2t_javaidx
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (java_idx (Java IDX download cache)). Coverage
  is generated by introspecting this repo's map `l2t_javaidx` (piiat_mitrecar.mappings); regenerate after
  any map change.
derived_from: disk image (Java IDX cache)
extractor:
  tool: log2timeline / Plaso
  parser: java_idx (Java IDX download cache)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tJavaIdx
identity:
  registry: piiat_mitrecar/spindle.yml
  version: 1
  entries:
  - name: l2t_javaidx
    kind: entity
    scope: intrinsic
    version: 1
data_model_coverage: http
mappings:
- object: http
  action: get
  notes: Derived by log2timeline / Plaso — java_idx (Java IDX download cache).
  fields:
  - hostname
  - url_domain
  - url_full
  - url_remainder
  - url_scheme
  field_provenance:
    hostname: image_hostname [direct]
    url_domain: url [inferred]
    url_full: url [direct]
    url_remainder: url [inferred]
    url_scheme: url [inferred]
other_coverage:
- 'native/join keys (http/get): artefact_file, data_type, idx_version, ip_address'
