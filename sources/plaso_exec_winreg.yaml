# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: plaso_exec_winreg
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (winreg (amcache/userassist/bam/appcompatcache)).
  Coverage is generated by introspecting this repo's map `plaso_exec_winreg` (piiat_mitrecar.mappings);
  regenerate after any map change.
derived_from: disk image (Windows registry hives)
extractor:
  tool: log2timeline / Plaso
  parser: winreg (amcache/userassist/bam/appcompatcache)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tWinreg
data_model_coverage: file, process
mappings:
- object: file
  action: create
  notes: Derived by log2timeline / Plaso — winreg (amcache/userassist/bam/appcompatcache).
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  - sha1_hash
  field_provenance:
    extension: full_path [derived]
    file_name: full_path [derived]
    file_path: full_path [direct]
    hostname: image_hostname [direct]
    sha1_hash: sha1 [direct]
- object: process
  action: create
  notes: Derived by log2timeline / Plaso — winreg (amcache/userassist/bam/appcompatcache).
  fields:
  - exe
  - hostname
  - image_path
  - sha1_hash
  - sid
  - user
  field_provenance:
    exe: full_path [derived]
    hostname: image_hostname [direct]
    image_path: full_path [direct]
    sha1_hash: sha1 [direct]
    sid: user_identifier [direct]
    user: username [direct]
other_coverage:
- 'native/join keys (file/create): artefact_file, artefact_sha256, compile_time, data_type, file_reference,
  key_path, program_identifier, timestamp_desc'
- 'native/join keys (process/create): artefact_file, artefact_sha256, control_set, data_type, entry_index,
  execution_inferred, file_reference, hive_user_sid, key_path, number_of_executions, program_identifier,
  time_meaning, timestamp_desc, value_name'
