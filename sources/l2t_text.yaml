# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: l2t_text
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (syslog (text) — SSH login lines). Coverage
  is generated by introspecting this repo's map `l2t_text` (piiat_mitrecar.mappings); regenerate after
  any map change.
derived_from: disk image (syslog text logs)
extractor:
  tool: log2timeline / Plaso
  parser: syslog (text) — SSH login lines
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tSyslog
- .L2tText
data_model_coverage: user_session
mappings:
- object: user_session
  action: login
  notes: Derived by log2timeline / Plaso — syslog (text) — SSH login lines.
  fields:
  - hostname
  - src_ip
  - src_port
  - user
other_coverage:
- 'native/join keys (user_session/login): authentication_method, data_type, hostname, protocol, reporter'
