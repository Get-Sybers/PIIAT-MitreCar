# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: l2t_srum
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (esedb/srum (System Resource Usage Monitor)).
  Coverage is generated by introspecting this repo's map `l2t_srum` (piiat_mitrecar.mappings); regenerate
  after any map change.
derived_from: disk image (SRUDB.dat ESE database)
extractor:
  tool: log2timeline / Plaso
  parser: esedb/srum (System Resource Usage Monitor)
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tEsedb
identity:
  registry: piiat_mitrecar/spindle.yml
  version: 1
  entries:
  - name: l2t_srum/application_usage
    kind: entity
    scope: intrinsic
    version: 1
  - name: l2t_srum/network_usage
    kind: entity
    scope: intrinsic
    version: 1
data_model_coverage: flow, process
mappings:
- object: flow
  action: message
  notes: Derived by log2timeline / Plaso — esedb/srum (System Resource Usage Monitor).
  fields:
  - exe
  - image_path
  - in_bytes
  - out_bytes
  - uid
  field_provenance:
    exe: application | application [coalesced]
    image_path: application [inferred]
    in_bytes: bytes_received [direct]
    out_bytes: bytes_sent [direct]
    uid: user_identifier [inferred]
- object: process
  action: create
  notes: Derived by log2timeline / Plaso — esedb/srum (System Resource Usage Monitor).
  fields:
  - exe
  - image_path
  - sid
  field_provenance:
    exe: application | application [coalesced]
    image_path: application [inferred]
    sid: user_identifier [inferred]
other_coverage:
- 'native/join keys (flow/message): application, data_type, identifier, interface_luid, user_identifier'
- 'native/join keys (process/create): application, data_type, face_time, foreground_bytes_read, foreground_bytes_written,
  foreground_cycle_time, identifier, interface_luid, user_identifier'
