# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: l2t_recyclebin
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (recycle_bin / recycle_bin_info2). Coverage
  is generated by introspecting this repo's map `l2t_recyclebin` (piiat_mitrecar.mappings); regenerate
  after any map change.
derived_from: disk image ($Recycle.Bin)
extractor:
  tool: log2timeline / Plaso
  parser: recycle_bin / recycle_bin_info2
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tRecycleBin
- .L2tRecycleBinInfo2
identity:
  registry: piiat_mitrecar/spindle.yml
  version: 1
  entries:
  - name: l2t_recyclebin
    kind: entity
    scope: intrinsic
    version: 1
data_model_coverage: file
mappings:
- object: file
  action: delete
  notes: Derived by log2timeline / Plaso — recycle_bin / recycle_bin_info2.
  fields:
  - extension
  - file_name
  - file_path
  - hostname
  - user
  field_provenance:
    extension: original_filename [derived]
    file_name: original_filename [derived]
    file_path: original_filename [direct]
    hostname: image_hostname [direct]
    user: username [direct]
other_coverage:
- 'native/join keys (file/delete): artefact_file, artefact_sha256, data_type, disk_id, drive_number, file_size,
  record_index, volume_id, volume_offset'
