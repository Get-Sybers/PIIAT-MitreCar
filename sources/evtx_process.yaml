# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: evtx_process
sensor_version: 0.1.0
sensor_developer: Eric Zimmerman (EZ Tools)
sensor_url: https://github.com/EricZimmerman/evtx
sensor_description: 'CAR events derived by EvtxECmd (Windows Event Log (.evtx) — Security channel (4688)).
  Alternate derivation(s): log2timeline / Plaso winevt/winevtx parser (via the l2t_winevt adapter). Coverage
  is generated by introspecting this repo''s map `evtx_process` (piiat_mitrecar.mappings); regenerate
  after any map change.'
derived_from: raw Windows Event Log (.evtx)
extractor:
  tool: EvtxECmd
  parser: Windows Event Log (.evtx) — Security channel (4688)
  url: https://github.com/EricZimmerman/evtx
  alternates:
  - log2timeline / Plaso winevt/winevtx parser (via the l2t_winevt adapter)
input_pattern:
- .L2tWinevt
- .L2tWinevtx
- _EvtxECmd_Output
data_model_coverage: process
mappings:
- object: process
  action: create
  notes: Derived by EvtxECmd — Windows Event Log (.evtx) — Security channel (4688).
  fields:
  - command_line
  - exe
  - hostname
  - image_path
  - integrity_level
  - parent_exe
  - parent_image_path
  - pid
  - ppid
  - sid
  - user
  field_provenance:
    command_line: CommandLine [direct]
    exe: NewProcessName [derived]
    hostname: Computer [derived]
    image_path: NewProcessName [direct]
    integrity_level: MandatoryLabel [inferred]
    parent_exe: ParentProcessName [derived]
    parent_image_path: ParentProcessName [direct]
    pid: NewProcessId [derived]
    ppid: ProcessId [derived]
    sid: TargetUserSid | SubjectUserSid [coalesced]
    user: TargetUserName | SubjectUserName [coalesced]
other_coverage:
- 'native/join keys (process/create): MandatoryLabel, SubjectLogonId, TokenElevationType'
