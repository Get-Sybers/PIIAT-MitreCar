# GENERATED by `python -m piiat_mitrecar.gen_sources` — do not edit.
# Source: piiat_mitrecar.mappings (coverage) + pipeline routing
# (extractor/input_pattern). Regenerate after any map change.
sensor_name: l2t_firefox_cache
sensor_version: 0.1.0
sensor_developer: log2timeline / Plaso project
sensor_url: https://github.com/log2timeline/plaso
sensor_description: CAR events derived by log2timeline / Plaso (firefox_cache). Coverage is generated
  by introspecting this repo's map `l2t_firefox_cache` (piiat_mitrecar.mappings); regenerate after any
  map change.
derived_from: disk image (Firefox cache)
extractor:
  tool: log2timeline / Plaso
  parser: firefox_cache
  url: https://github.com/log2timeline/plaso
input_pattern:
- .L2tFirefoxCache
data_model_coverage: http
mappings:
- object: http
  action: get
  notes: Derived by log2timeline / Plaso — firefox_cache.
  fields:
  - hostname
  - response_status_code
  - url_domain
  - url_full
  - url_remainder
  - url_scheme
  field_provenance:
    hostname: image_hostname [direct]
    response_status_code: response_code [derived]
    url_domain: url | url [inferred]
    url_full: url | url [coalesced]
    url_remainder: url | url [inferred]
    url_scheme: url | url [inferred]
- object: http
  action: post
  notes: Derived by log2timeline / Plaso — firefox_cache.
  fields:
  - hostname
  - response_status_code
  - url_domain
  - url_full
  - url_remainder
  - url_scheme
  field_provenance:
    hostname: image_hostname [direct]
    response_status_code: response_code [derived]
    url_domain: url | url [inferred]
    url_full: url | url [coalesced]
    url_remainder: url | url [inferred]
    url_scheme: url | url [inferred]
- object: http
  action: put
  notes: Derived by log2timeline / Plaso — firefox_cache.
  fields:
  - hostname
  - response_status_code
  - url_domain
  - url_full
  - url_remainder
  - url_scheme
  field_provenance:
    hostname: image_hostname [direct]
    response_status_code: response_code [derived]
    url_domain: url | url [inferred]
    url_full: url | url [coalesced]
    url_remainder: url | url [inferred]
    url_scheme: url | url [inferred]
other_coverage:
- 'native/join keys (http/get): artefact_file, data_size, data_type, fetch_count'
- 'native/join keys (http/post): artefact_file, data_size, data_type, fetch_count'
- 'native/join keys (http/put): artefact_file, data_size, data_type, fetch_count'
