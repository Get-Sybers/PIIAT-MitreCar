# GENERATED by `python model/generate.py` — do not edit by hand.
# The `registry` CAR object: reconstructed from piiat_mitrecar.carmodel /
# store.HEADER (properties) and build_data_model.build_superset (car_action),
# enriched with field descriptions from the pinned car data_model/registry.yaml.
# Source of truth (pinned submodules):
#   third_party/car @ 1b922fe1527d956e222a99473472e594f10f610b
#   third_party/attack-datasources @ 5d50f731de441eb09078623a2c29cc3420a01949

name: registry
description: The registry is a system-defined database in which applications and system components store
  and retrieve configuration data. The data stored in the registry varies according to the version of
  Microsoft Windows. Applications use the registry API to retrieve, modify, or delete registry data.
car_action:
- access
- add
- create
- delete
- key_edit
- modify
- remove
- value_edit
properties:
  common_header:
  - timestamp
  - car_action
  - guid
  - owning_guid
  - link_confidence
  - source_artefact
  - source_host
  - native
  object_fields:
  - name: fqdn
    description: The fully qualified domain name for the host on which the registry access took place.
    example: HOST1.EXAMPLE_DOMAIN.COM
  - name: hostname
    description: The hostname of the host, without the domain.
    example: HOST1
  - name: key
    description: The registry key of the event. Similar to a folder in a traditional file system.
    example: HKLM\SYSTEM\CurrentControlSet\services\RpcSs
  - name: value
    description: The descriptive name for the data being stored.
    example: InstalledVersion
  - name: data
    description: The content of `value`, typically a text string.
    example: \%SystemRoot%\system32\svchost.exe -k rpcss
  - name: type
    description: The type of data being stored in `value`. Types include binary data, 32 bit numbers,
      strings, etc.
    example: REG_BINARY
  - name: hive
    description: The logical group of keys, subkeys, and values in the registry.
    example: HKEY_CURRENT_USER
  - name: image_path
    description: Inherited from the [process](https://car.mitre.org/data_model/process) that made the
      registry access.
    example: C:\path\to\example.exe
  - name: pid
    description: Inherited from the [process](https://car.mitre.org/data_model/process) that made the
      registry access.
    example: 738
  - name: user
    description: The user in the context of the process that performed the action on the registry key.
    example: HOST1\LOCALUSER
  - name: new_content
    description: The data within the new value, or the new name of a key, after an edit event.
    example: \%SystemRoot%\system32\svchost.exe, HKLM\SYSTEM\CurrentControlSet\services\RpcSs
