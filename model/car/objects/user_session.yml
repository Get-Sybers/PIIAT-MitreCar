# GENERATED by `python model/generate.py` — do not edit by hand.
# The `user_session` CAR object: reconstructed from piiat_mitrecar.carmodel /
# store.HEADER (properties) and build_data_model.build_superset (car_action),
# enriched with field descriptions from the pinned car data_model/user_session.yaml.
# Source of truth (pinned submodules):
#   third_party/car @ 1b922fe1527d956e222a99473472e594f10f610b
#   third_party/attack-datasources @ 5d50f731de441eb09078623a2c29cc3420a01949

name: user_session
description: User sessions are the user activities undertaken on the computer in the course of conducting
  standard user actions.
car_action:
- create
- lock
- login
- logout
- metadata
- reconnect
- terminate
- unlock
properties:
  common_header:
  - timestamp
  - car_action
  - guid
  - owning_guid
  - link_confidence
  - source_artefact
  - source_host
  - native
  object_fields:
  - name: dest_ip
    description: The destination IP address of the user session. Only applicable to remote or RDP sessions.
    example: 192.168.1.5
  - name: dest_port
    description: The destination port of the user session. Only applicable to remote or RDP sessions.
    example: 1900
  - name: hostname
    description: The hostname of the host, without the domain.
    example: HOST1
  - name: login_id
    description: A hex value corresponding to the session. The logon id will persist until logout occurs.
    example: 1008115
  - name: src_ip
    description: The source IP address of the user session. Only applicable to remote or RDP sessions.
    example: 10.0.0.54
  - name: src_port
    description: The source port of the user session. Only applicable to remote or RDP sessions.
    example: 50438
  - name: user
    description: The user affiliated with the session. May be a local, domain or SYSTEM user.
    example: HOST1\LOCALUSER
  - name: login_type
    description: The type of login that was accomplished or attempted
    example: interactive,local,rdp,remote
  - name: uid
    description: ID or SID of the user for which a session event ocurred
    example: S-1-5-18
  - name: login_successful
    description: Boolean indicator of whether a login attempt was successful
    example: false
