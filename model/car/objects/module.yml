# GENERATED by `python model/generate.py` — do not edit by hand.
# The `module` CAR object: reconstructed from piiat_mitrecar.carmodel /
# store.HEADER (properties) and build_data_model.build_superset (car_action),
# enriched with field descriptions from the pinned car data_model/module.yaml.
# Source of truth (pinned submodules):
#   third_party/car @ 1b922fe1527d956e222a99473472e594f10f610b
#   third_party/attack-datasources @ 5d50f731de441eb09078623a2c29cc3420a01949

name: module
description: Modules correspond to executable (and potentially non-executable) content, and are loaded
  as a contiguous region of memory into the address space of a process. Each process will have the main
  image loaded as a module and shared libraries (DLLs in Windows) and their dependencies.
car_action:
- content
- load
- metadata
- unload
properties:
  common_header:
  - timestamp
  - car_action
  - guid
  - owning_guid
  - link_confidence
  - source_artefact
  - source_host
  - native
  object_fields:
  - name: fqdn
    description: The fully qualified domain name of the host. Contains the hostname appended with the
      domain.
    example: HOST1.EXAMPLE_DOMAIN.COM
  - name: hostname
    description: The hostname of the host, without the domain.
    example: HOST1
  - name: base_address
    description: A hex address indicating where the module is loaded into the process’s virtual address
      space.
    example: 18446735277684027392
  - name: image_path
    description: The file system location of the process image.
    example: C:\path\to\example.exe
  - name: md5_hash
    description: The MD5 hash of the contents of the file located at `module_path`. The field is in hex
      notation, without the 0x prefix.
    example: 5eb63bbbe01eeed093cb22bb8f5acdc3
  - name: sha1_hash
    description: The SHA1 hash of the contents of the file located at `image_path`.
    example: 2aae6c35c94fcfb415dbe95f408b9ce91ee846ed
  - name: sha256_hash
    description: The SHA256 hash of the contents of the file located at `image_path`.
    example: 68e656b251e67e8358bef8483ab0d51c6619f3e7a1a9f0e75838d41ff368f728
  - name: module_path
    description: The full file system path to the module loaded into the memory space of the process.
    example: C:\windows\system32\kernel32.exe
  - name: module_name
    description: The name of the file where the module is loaded on disk. This is also the string that
      is used internally by the program to lookup information about the module.
    example: kernel32.exe
  - name: pid
    description: Process ID of the process in which the module is loaded (or unloaded).
    example: 738
  - name: signer
    description: The name of the organization which signed the module.
    example: Microsoft Corporation
  - name: tid
    description: The thread ID of the thread responsible for the load or unload event.
    example: 50
  - name: signature_valid
    description: Boolean indicator of whether the signature is current and not revoked
    example: true
