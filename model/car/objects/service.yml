# GENERATED by `python model/generate.py` — do not edit by hand.
# The `service` CAR object: reconstructed from piiat_mitrecar.carmodel /
# store.HEADER (properties) and build_data_model.build_superset (car_action),
# enriched with field descriptions from the pinned car data_model/service.yaml.
# Source of truth (pinned submodules):
#   third_party/car @ 1b922fe1527d956e222a99473472e594f10f610b
#   third_party/attack-datasources @ 5d50f731de441eb09078623a2c29cc3420a01949

name: service
description: Services, or a service application, can be started automatically at system boot, by a user
  through the services control panel applet, or by an application that uses service functions. Services
  can execute even when no user is logged into the system.
car_action:
- access
- create
- delete
- enumerate
- metadata
- modify
- pause
- start
- stop
properties:
  common_header:
  - timestamp
  - car_action
  - guid
  - owning_guid
  - link_confidence
  - source_artefact
  - source_host
  - native
  object_fields:
  - name: fqdn
    description: The fully qualified domain name of the host. Contains the hostname appended with the
      domain.
    example: HOST1.EXAMPLE_DOMAIN.COM
  - name: hostname
    description: The hostname of the host, without the domain.
    example: HOST1
  - name: user
    description: The user token that service was created with.
    example: HOST1\LOCALUSER
  - name: command_line
    description: The command line that service is started with.
    example: C:\windows\system32\svchost.exe -k rpcss
  - name: exe
    description: The executable for the service.
    example: svchost.exe
  - name: image_path
    description: Where in the file system the service executable is located.
    example: C:\path\to\example.exe
  - name: name
    description: The name of the service.
    example: RpcSs
  - name: pid
    description: The process ID for the process of the service, represented in decimal notation.
    example: 718
  - name: ppid
    description: The process ID of the process’s parent or the service, represented in decimal notation.
      In the parent process, this will be the pid field.
    example: 1860
  - name: uid
    description: The ID of SID of the user who acted on the service
    example: S-1-5-18
